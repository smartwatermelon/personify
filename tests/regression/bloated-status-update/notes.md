# Notes

Exercises: V (primary), W (primary), Z (primary), X (guard), N (sign-off).

Built from the worked example already in SKILL.md's Work register section,
extended with a second paragraph that is pure group Z. The original worked
example only exercises V and W: it has no component inventory, so it cannot
catch a Z regression at all. That is the gap this case fills.

## What a regression looks like

The most likely regression is not that the output gets longer. It is that the
second paragraph survives as a tightened list of components:

> refactored the indexer, added a queue consumer with a dead-letter path and
> metrics.

That is a correct application of V to text that needed Z. It is shorter, it
reads as human, every noun is accurate, and it still never says what anyone
got. If a future edit produces that, V is being applied where Z should have
gone first, which is exactly the priority slip #49 is filed to prevent.

## Classification

This is a message to colleagues, so classification step 2, work register.
It is under 800 words and so cannot reach step 3. The long-form loophole is
covered by the sibling case, which is deliberately long.
